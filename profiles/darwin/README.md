# macOS profile

The public macOS profile contains only portable command-line tooling.

Keep macOS application choices, Homebrew taps, personal package lists, agent
providers, and machine-specific paths in untracked local overrides. The common
shell profile intentionally does not assume `/opt/homebrew`, `~/Library/pnpm`,
or any model-provider path.
